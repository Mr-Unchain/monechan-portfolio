import { workSchema } from './work';
import { aboutPageSchema } from './aboutPage';
import { contactPageSchema } from './contactPage';

export const schemaTypes = [workSchema, aboutPageSchema, contactPageSchema];
